apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentgate.fullname" . }}
  labels:
    {{- include "agentgate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agentgate.selectorLabels" . | nindent 4 }}
