# NOTE: This package is not yet published to PyPI. The live, publicly
# installable Python integration is `langchain-suprawall`, located in
# plugins/langchain-py/. This SDK is under development for future release.

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "suprawall-sdk"
version = "0.1.0"
description = "The open-source core for AI agent security and compliance"
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.9"
authors = [{ name = "SupraWall Contributors" }]
keywords = ["ai", "agents", "security", "mcp", "langchain", "openai", "gateway", "policy"]
dependencies = [
    "httpx>=0.27.0",
]

[project.optional-dependencies]
openai    = ["openai-agents>=0.0.3"]
dev       = ["pytest>=7.0.0"]
all       = ["openai-agents>=0.0.3", "pytest>=7.0.0"]

[project.urls]
Homepage      = "https://supra-wall.com"
Repository    = "https://github.com/wiserautomation/SupraWall"
"Bug Tracker" = "https://github.com/wiserautomation/SupraWall/issues"

[tool.hatch.build.targets.wheel]
packages = ["suprawall"]

[tool.hatch.build.targets.sdist]
exclude = ["venv", "tests", ".env"]
