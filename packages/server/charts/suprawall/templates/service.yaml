apiVersion: v1
kind: Service
metadata:
  name: {{ include "suprawall.fullname" . }}
  labels:
    {{- include "suprawall.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "suprawall.selectorLabels" . | nindent 4 }}
